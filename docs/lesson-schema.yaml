definitions:
  createLesson:
    type: object
    properties:
      title:
        type: string
      description:
        type: string
      content:
        type: string
      category?:
        type: string
      resourceType?:
        type: string
        enum:
          - lesson
      attachments?:
        type: array
        items:
          type: string
